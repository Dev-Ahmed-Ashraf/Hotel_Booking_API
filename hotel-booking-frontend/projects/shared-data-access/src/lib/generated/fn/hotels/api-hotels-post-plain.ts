/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CreateHotelDto } from '../../models/create-hotel-dto';
import { HotelDtoApiResponse } from '../../models/hotel-dto-api-response';

export interface ApiHotelsPost$Plain$Params {
  
    /**
     * The hotel details to create, including name, description, address, city, country, and rating.
     */
    body?: CreateHotelDto
}

export function apiHotelsPost$Plain(http: HttpClient, rootUrl: string, params?: ApiHotelsPost$Plain$Params, context?: HttpContext): Observable<StrictHttpResponse<HotelDtoApiResponse>> {
  const rb = new RequestBuilder(rootUrl, apiHotelsPost$Plain.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'text', accept: 'text/plain', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<HotelDtoApiResponse>;
    })
  );
}

apiHotelsPost$Plain.PATH = '/api/Hotels';
