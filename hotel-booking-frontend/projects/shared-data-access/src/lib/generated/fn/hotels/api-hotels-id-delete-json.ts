/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { StringApiResponse } from '../../models/string-api-response';

export interface ApiHotelsIdDelete$Json$Params {

/**
 * Hotel ID to delete
 */
  id: number;

/**
 * Soft delete (default: true)
 */
  isSoft?: boolean;

/**
 * Skip active booking checks (default: false)
 */
  forceDelete?: boolean;
}

export function apiHotelsIdDelete$Json(http: HttpClient, rootUrl: string, params: ApiHotelsIdDelete$Json$Params, context?: HttpContext): Observable<StrictHttpResponse<StringApiResponse>> {
  const rb = new RequestBuilder(rootUrl, apiHotelsIdDelete$Json.PATH, 'delete');
  if (params) {
    rb.path('id', params.id, {});
    rb.query('isSoft', params.isSoft, {});
    rb.query('forceDelete', params.forceDelete, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'text/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<StringApiResponse>;
    })
  );
}

apiHotelsIdDelete$Json.PATH = '/api/Hotels/{id}';
