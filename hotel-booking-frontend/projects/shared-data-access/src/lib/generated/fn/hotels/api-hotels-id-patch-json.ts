/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { HotelDtoApiResponse } from '../../models/hotel-dto-api-response';
import { UpdateHotelDto } from '../../models/update-hotel-dto';

export interface ApiHotelsIdPatch$Json$Params {

/**
 * The ID of the hotel to update.
 */
  id: number;
  
    /**
     * The fields you want to update.  
     * You can send only the properties that need changes (partial update supported).
     */
    body?: UpdateHotelDto
}

export function apiHotelsIdPatch$Json(http: HttpClient, rootUrl: string, params: ApiHotelsIdPatch$Json$Params, context?: HttpContext): Observable<StrictHttpResponse<HotelDtoApiResponse>> {
  const rb = new RequestBuilder(rootUrl, apiHotelsIdPatch$Json.PATH, 'patch');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'text/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<HotelDtoApiResponse>;
    })
  );
}

apiHotelsIdPatch$Json.PATH = '/api/Hotels/{id}';
