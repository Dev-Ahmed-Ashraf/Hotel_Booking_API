/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { HotelDtoPagedListApiResponse } from '../../models/hotel-dto-paged-list-api-response';

export interface ApiHotelsGet$Json$Params {

/**
 * The page number for pagination (default: 1).
 */
  pageNumber?: number;

/**
 * The number of hotels per page (default: 10).
 */
  pageSize?: number;

/**
 * Optional filter by city name.
 */
  city?: string;

/**
 * Optional filter by country name.
 */
  country?: string;

/**
 * Optional minimum rating filter (e.g., 3).
 */
  minRating?: number;

/**
 * Optional maximum rating filter (e.g., 5).
 */
  maxRating?: number;

/**
 * Whether to include soft-deleted hotels.
 */
  includeDeleted?: boolean;
}

export function apiHotelsGet$Json(http: HttpClient, rootUrl: string, params?: ApiHotelsGet$Json$Params, context?: HttpContext): Observable<StrictHttpResponse<HotelDtoPagedListApiResponse>> {
  const rb = new RequestBuilder(rootUrl, apiHotelsGet$Json.PATH, 'get');
  if (params) {
    rb.query('pageNumber', params.pageNumber, {});
    rb.query('pageSize', params.pageSize, {});
    rb.query('city', params.city, {});
    rb.query('country', params.country, {});
    rb.query('minRating', params.minRating, {});
    rb.query('maxRating', params.maxRating, {});
    rb.query('includeDeleted', params.includeDeleted, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'text/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<HotelDtoPagedListApiResponse>;
    })
  );
}

apiHotelsGet$Json.PATH = '/api/Hotels';
