/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ReviewDtoPagedListApiResponse } from '../../models/review-dto-paged-list-api-response';

export interface ApiReviewsGet$Plain$Params {

/**
 * The page number for pagination (default: 1).
 */
  pageNumber?: number;

/**
 * The number of reviews per page (default: 10).
 */
  pageSize?: number;

/**
 * Optional filter by hotel ID.
 */
  hotelId?: number;

/**
 * Optional filter by user ID.
 */
  userId?: number;

/**
 * Optional filter by minimum rating (1-5).
 */
  minRating?: number;

/**
 * Optional filter by maximum rating (1-5).
 */
  maxRating?: number;

/**
 * Whether to include soft-deleted reviews.
 */
  includeDeleted?: boolean;
}

export function apiReviewsGet$Plain(http: HttpClient, rootUrl: string, params?: ApiReviewsGet$Plain$Params, context?: HttpContext): Observable<StrictHttpResponse<ReviewDtoPagedListApiResponse>> {
  const rb = new RequestBuilder(rootUrl, apiReviewsGet$Plain.PATH, 'get');
  if (params) {
    rb.query('pageNumber', params.pageNumber, {});
    rb.query('pageSize', params.pageSize, {});
    rb.query('hotelId', params.hotelId, {});
    rb.query('userId', params.userId, {});
    rb.query('minRating', params.minRating, {});
    rb.query('maxRating', params.maxRating, {});
    rb.query('includeDeleted', params.includeDeleted, {});
  }

  return http.request(
    rb.build({ responseType: 'text', accept: 'text/plain', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ReviewDtoPagedListApiResponse>;
    })
  );
}

apiReviewsGet$Plain.PATH = '/api/Reviews';
