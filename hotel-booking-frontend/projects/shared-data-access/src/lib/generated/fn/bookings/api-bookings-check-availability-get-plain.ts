/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { BooleanApiResponse } from '../../models/boolean-api-response';

export interface ApiBookingsCheckAvailabilityGet$Plain$Params {

/**
 * The ID of the room to check.
 */
  roomId?: number;

/**
 * The check-in date.
 */
  checkInDate?: string;

/**
 * The check-out date.
 */
  checkOutDate?: string;
}

export function apiBookingsCheckAvailabilityGet$Plain(http: HttpClient, rootUrl: string, params?: ApiBookingsCheckAvailabilityGet$Plain$Params, context?: HttpContext): Observable<StrictHttpResponse<BooleanApiResponse>> {
  const rb = new RequestBuilder(rootUrl, apiBookingsCheckAvailabilityGet$Plain.PATH, 'get');
  if (params) {
    rb.query('roomId', params.roomId, {});
    rb.query('checkInDate', params.checkInDate, {});
    rb.query('checkOutDate', params.checkOutDate, {});
  }

  return http.request(
    rb.build({ responseType: 'text', accept: 'text/plain', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<BooleanApiResponse>;
    })
  );
}

apiBookingsCheckAvailabilityGet$Plain.PATH = '/api/Bookings/check-availability';
