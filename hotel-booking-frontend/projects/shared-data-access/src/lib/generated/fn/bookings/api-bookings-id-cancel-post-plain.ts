/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CancelBookingDto } from '../../models/cancel-booking-dto';
import { StringApiResponse } from '../../models/string-api-response';

export interface ApiBookingsIdCancelPost$Plain$Params {

/**
 * The ID of the booking to cancel.
 */
  id: number;
  
    /**
     * Cancellation details including reason.
     */
    body?: CancelBookingDto
}

export function apiBookingsIdCancelPost$Plain(http: HttpClient, rootUrl: string, params: ApiBookingsIdCancelPost$Plain$Params, context?: HttpContext): Observable<StrictHttpResponse<StringApiResponse>> {
  const rb = new RequestBuilder(rootUrl, apiBookingsIdCancelPost$Plain.PATH, 'post');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'text', accept: 'text/plain', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<StringApiResponse>;
    })
  );
}

apiBookingsIdCancelPost$Plain.PATH = '/api/Bookings/{id}/cancel';
