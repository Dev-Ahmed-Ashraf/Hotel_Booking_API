/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { BookingDtoApiResponse } from '../../models/booking-dto-api-response';
import { CreateBookingDto } from '../../models/create-booking-dto';

export interface ApiBookingsPost$Plain$Params {

/**
 * The ID of the user making the booking
 */
  userId?: number;
  
    /**
     * The booking details to create
     */
    body?: CreateBookingDto
}

export function apiBookingsPost$Plain(http: HttpClient, rootUrl: string, params?: ApiBookingsPost$Plain$Params, context?: HttpContext): Observable<StrictHttpResponse<BookingDtoApiResponse>> {
  const rb = new RequestBuilder(rootUrl, apiBookingsPost$Plain.PATH, 'post');
  if (params) {
    rb.query('userId', params.userId, {});
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'text', accept: 'text/plain', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<BookingDtoApiResponse>;
    })
  );
}

apiBookingsPost$Plain.PATH = '/api/Bookings';
