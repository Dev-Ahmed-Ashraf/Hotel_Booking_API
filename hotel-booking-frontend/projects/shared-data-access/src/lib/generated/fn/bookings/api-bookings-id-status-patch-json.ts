/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { BookingDtoApiResponse } from '../../models/booking-dto-api-response';
import { ChangeBookingStatusDto } from '../../models/change-booking-status-dto';

export interface ApiBookingsIdStatusPatch$Json$Params {

/**
 * The ID of the booking to update.
 */
  id: number;
  
    /**
     * The new status and optional notes.
     */
    body?: ChangeBookingStatusDto
}

export function apiBookingsIdStatusPatch$Json(http: HttpClient, rootUrl: string, params: ApiBookingsIdStatusPatch$Json$Params, context?: HttpContext): Observable<StrictHttpResponse<BookingDtoApiResponse>> {
  const rb = new RequestBuilder(rootUrl, apiBookingsIdStatusPatch$Json.PATH, 'patch');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'text/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<BookingDtoApiResponse>;
    })
  );
}

apiBookingsIdStatusPatch$Json.PATH = '/api/Bookings/{id}/status';
