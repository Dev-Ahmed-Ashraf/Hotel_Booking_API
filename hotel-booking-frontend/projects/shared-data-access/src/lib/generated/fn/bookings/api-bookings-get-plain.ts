/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { BookingDtoPagedListApiResponse } from '../../models/booking-dto-paged-list-api-response';
import { BookingStatus } from '../../models/booking-status';

export interface ApiBookingsGet$Plain$Params {

/**
 * The page number for pagination (default: 1).
 */
  pageNumber?: number;

/**
 * The number of bookings per page (default: 10).
 */
  pageSize?: number;

/**
 * Optional filter by hotel ID.
 */
  hotelId?: number;

/**
 * Optional filter by user ID.
 */
  userId?: number;

/**
 * Optional filter by booking status.
 */
  status?: BookingStatus;

/**
 * Optional filter by start date.
 */
  startDate?: string;

/**
 * Optional filter by end date.
 */
  endDate?: string;

/**
 * Whether to include soft-deleted bookings.
 */
  includeDeleted?: boolean;
}

export function apiBookingsGet$Plain(http: HttpClient, rootUrl: string, params?: ApiBookingsGet$Plain$Params, context?: HttpContext): Observable<StrictHttpResponse<BookingDtoPagedListApiResponse>> {
  const rb = new RequestBuilder(rootUrl, apiBookingsGet$Plain.PATH, 'get');
  if (params) {
    rb.query('pageNumber', params.pageNumber, {});
    rb.query('pageSize', params.pageSize, {});
    rb.query('hotelId', params.hotelId, {});
    rb.query('userId', params.userId, {});
    rb.query('status', params.status, {});
    rb.query('startDate', params.startDate, {});
    rb.query('endDate', params.endDate, {});
    rb.query('includeDeleted', params.includeDeleted, {});
  }

  return http.request(
    rb.build({ responseType: 'text', accept: 'text/plain', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<BookingDtoPagedListApiResponse>;
    })
  );
}

apiBookingsGet$Plain.PATH = '/api/Bookings';
