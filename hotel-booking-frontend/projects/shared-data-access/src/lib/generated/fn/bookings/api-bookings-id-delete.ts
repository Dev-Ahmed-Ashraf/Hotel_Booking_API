/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';


export interface ApiBookingsIdDelete$Params {

/**
 * Booking ID to delete
 */
  id: number;

/**
 * If true, marks the booking as deleted instead of removing it permanently (default: true)
 */
  isSoft?: boolean;

/**
 * If true, forces deletion even if booking is active (default: false)
 */
  forceDelete?: boolean;
}

export function apiBookingsIdDelete(http: HttpClient, rootUrl: string, params: ApiBookingsIdDelete$Params, context?: HttpContext): Observable<StrictHttpResponse<void>> {
  const rb = new RequestBuilder(rootUrl, apiBookingsIdDelete.PATH, 'delete');
  if (params) {
    rb.path('id', params.id, {});
    rb.query('isSoft', params.isSoft, {});
    rb.query('forceDelete', params.forceDelete, {});
  }

  return http.request(
    rb.build({ responseType: 'text', accept: '*/*', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return (r as HttpResponse<any>).clone({ body: undefined }) as StrictHttpResponse<void>;
    })
  );
}

apiBookingsIdDelete.PATH = '/api/Bookings/{id}';
