/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { BookingPriceResponseDtoApiResponse } from '../../models/booking-price-response-dto-api-response';

export interface ApiBookingsCalculatePriceGet$Json$Params {

/**
 * The ID of the room.
 */
  roomId?: number;

/**
 * The check-in date.
 */
  checkInDate?: string;

/**
 * The check-out date.
 */
  checkOutDate?: string;
}

export function apiBookingsCalculatePriceGet$Json(http: HttpClient, rootUrl: string, params?: ApiBookingsCalculatePriceGet$Json$Params, context?: HttpContext): Observable<StrictHttpResponse<BookingPriceResponseDtoApiResponse>> {
  const rb = new RequestBuilder(rootUrl, apiBookingsCalculatePriceGet$Json.PATH, 'get');
  if (params) {
    rb.query('roomId', params.roomId, {});
    rb.query('checkInDate', params.checkInDate, {});
    rb.query('checkOutDate', params.checkOutDate, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'text/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<BookingPriceResponseDtoApiResponse>;
    })
  );
}

apiBookingsCalculatePriceGet$Json.PATH = '/api/Bookings/calculate-price';
