/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { RoomDtoListApiResponse } from '../../models/room-dto-list-api-response';
import { RoomType } from '../../models/room-type';

export interface ApiRoomsAvailableGet$Json$Params {

/**
 * Optional hotel ID filter.
 */
  hotelId?: number;

/**
 * The check-in date.
 */
  checkInDate?: string;

/**
 * The check-out date.
 */
  checkOutDate?: string;

/**
 * Optional room type filter.
 */
  type?: RoomType;

/**
 * Optional minimum capacity filter.
 */
  minCapacity?: number;

/**
 * Optional maximum price filter.
 */
  maxPrice?: number;
}

export function apiRoomsAvailableGet$Json(http: HttpClient, rootUrl: string, params?: ApiRoomsAvailableGet$Json$Params, context?: HttpContext): Observable<StrictHttpResponse<RoomDtoListApiResponse>> {
  const rb = new RequestBuilder(rootUrl, apiRoomsAvailableGet$Json.PATH, 'get');
  if (params) {
    rb.query('hotelId', params.hotelId, {});
    rb.query('checkInDate', params.checkInDate, {});
    rb.query('checkOutDate', params.checkOutDate, {});
    rb.query('type', params.type, {});
    rb.query('minCapacity', params.minCapacity, {});
    rb.query('maxPrice', params.maxPrice, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'text/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<RoomDtoListApiResponse>;
    })
  );
}

apiRoomsAvailableGet$Json.PATH = '/api/Rooms/available';
