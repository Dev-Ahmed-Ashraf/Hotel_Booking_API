/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { RoomDtoApiResponse } from '../../models/room-dto-api-response';
import { UpdateRoomDto } from '../../models/update-room-dto';

export interface ApiRoomsIdPatch$Json$Params {

/**
 * The ID of the room to update.
 */
  id: number;
  
    /**
     * The fields you want to update.  
     * You can send only the properties that need changes (partial update supported).
     */
    body?: UpdateRoomDto
}

export function apiRoomsIdPatch$Json(http: HttpClient, rootUrl: string, params: ApiRoomsIdPatch$Json$Params, context?: HttpContext): Observable<StrictHttpResponse<RoomDtoApiResponse>> {
  const rb = new RequestBuilder(rootUrl, apiRoomsIdPatch$Json.PATH, 'patch');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'text/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<RoomDtoApiResponse>;
    })
  );
}

apiRoomsIdPatch$Json.PATH = '/api/Rooms/{id}';
