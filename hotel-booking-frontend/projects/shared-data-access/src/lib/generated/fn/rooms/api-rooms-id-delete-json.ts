/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { StringApiResponse } from '../../models/string-api-response';

export interface ApiRoomsIdDelete$Json$Params {

/**
 * Room ID to delete.
 */
  id: number;

/**
 * If true, performs a soft delete (default).  
 * Soft delete keeps the room in the database but marks it as deleted.
 */
  isSoft?: boolean;

/**
 * If true, allows deletion even if the room has active bookings.  
 * Use with caution.
 */
  forceDelete?: boolean;
}

export function apiRoomsIdDelete$Json(http: HttpClient, rootUrl: string, params: ApiRoomsIdDelete$Json$Params, context?: HttpContext): Observable<StrictHttpResponse<StringApiResponse>> {
  const rb = new RequestBuilder(rootUrl, apiRoomsIdDelete$Json.PATH, 'delete');
  if (params) {
    rb.path('id', params.id, {});
    rb.query('isSoft', params.isSoft, {});
    rb.query('forceDelete', params.forceDelete, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'text/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<StringApiResponse>;
    })
  );
}

apiRoomsIdDelete$Json.PATH = '/api/Rooms/{id}';
