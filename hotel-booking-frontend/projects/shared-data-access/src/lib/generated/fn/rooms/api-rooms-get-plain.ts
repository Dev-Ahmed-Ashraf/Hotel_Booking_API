/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { RoomDtoPagedListApiResponse } from '../../models/room-dto-paged-list-api-response';
import { RoomType } from '../../models/room-type';

export interface ApiRoomsGet$Plain$Params {

/**
 * The page number for pagination (default: 1).
 */
  pageNumber?: number;

/**
 * The number of rooms per page (default: 10).
 */
  pageSize?: number;

/**
 * Optional filter by hotel ID.
 */
  hotelId?: number;

/**
 * Optional filter by hotel name.
 */
  hotelName?: string;

/**
 * Optional filter by room number.
 */
  roomNumber?: string;

/**
 * Optional filter by room type.
 */
  type?: RoomType;

/**
 * Optional minimum price filter.
 */
  minPrice?: number;

/**
 * Optional maximum price filter.
 */
  maxPrice?: number;

/**
 * Optional minimum capacity filter.
 */
  capacity?: number;

/**
 * Whether to include soft-deleted rooms.
 */
  includeDeleted?: boolean;
}

export function apiRoomsGet$Plain(http: HttpClient, rootUrl: string, params?: ApiRoomsGet$Plain$Params, context?: HttpContext): Observable<StrictHttpResponse<RoomDtoPagedListApiResponse>> {
  const rb = new RequestBuilder(rootUrl, apiRoomsGet$Plain.PATH, 'get');
  if (params) {
    rb.query('pageNumber', params.pageNumber, {});
    rb.query('pageSize', params.pageSize, {});
    rb.query('hotelId', params.hotelId, {});
    rb.query('hotelName', params.hotelName, {});
    rb.query('roomNumber', params.roomNumber, {});
    rb.query('type', params.type, {});
    rb.query('minPrice', params.minPrice, {});
    rb.query('maxPrice', params.maxPrice, {});
    rb.query('capacity', params.capacity, {});
    rb.query('includeDeleted', params.includeDeleted, {});
  }

  return http.request(
    rb.build({ responseType: 'text', accept: 'text/plain', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<RoomDtoPagedListApiResponse>;
    })
  );
}

apiRoomsGet$Plain.PATH = '/api/Rooms';
