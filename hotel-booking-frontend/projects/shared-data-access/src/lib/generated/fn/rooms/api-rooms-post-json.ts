/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CreateRoomDto } from '../../models/create-room-dto';
import { RoomDtoApiResponse } from '../../models/room-dto-api-response';

export interface ApiRoomsPost$Json$Params {
  
    /**
     * The room details to create, including hotel ID, room number, type, price, capacity, and description.
     */
    body?: CreateRoomDto
}

export function apiRoomsPost$Json(http: HttpClient, rootUrl: string, params?: ApiRoomsPost$Json$Params, context?: HttpContext): Observable<StrictHttpResponse<RoomDtoApiResponse>> {
  const rb = new RequestBuilder(rootUrl, apiRoomsPost$Json.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'text/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<RoomDtoApiResponse>;
    })
  );
}

apiRoomsPost$Json.PATH = '/api/Rooms';
